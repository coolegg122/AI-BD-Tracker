{
  "name": "bd-project-management",
  "private": true,
  "scripts": {
    "build": "cd ai-bd-tracker && npm run build",
    "install:frontend": "cd ai-bd-tracker && npm install",
    "vercel-build": "cd ai-bd-tracker && npm ci && npm run build"
  }
}
