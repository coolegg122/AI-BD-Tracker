{
  "name": "bd-project-management",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix ai-bd-tracker",
    "install:frontend": "npm install --prefix ai-bd-tracker",
    "vercel-build": "npm install --prefix ai-bd-tracker && npm run build --prefix ai-bd-tracker"
  }
}
